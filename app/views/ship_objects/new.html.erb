<div class="container ship-object-new-container mt-4">
  <h2>Ajouter un objet à l'inventaire du vaisseau</h2>
  <%= form_for [@ship, @ship_object] do |f| %>
    <div class="form-group mb-2">
      <%= f.label :name, "Nom de l'objet", class: "form-label" %>
      <%= f.text_field :name, class: "form-control" %>
    </div>
    <div class="form-group mb-2">
      <%= f.label :quantity, "Quantité", class: "form-label" %>
      <%= f.number_field :quantity, class: "form-control", min: 1, value: 1 %>
    </div>
    <div class="form-group mb-2">
      <%= f.label :description, "Description", class: "form-label" %>
      <%= f.text_field :description, class: "form-control" %>
    </div>
    <div class="form-group text-center mt-3">
      <%= f.submit "Ajouter", class: "btn btn-success btn-lg" %>
      <%= link_to "Annuler", ship_path(@ship), class: "btn btn-secondary btn-lg" %>
    </div>
  <% end %>
</div>
